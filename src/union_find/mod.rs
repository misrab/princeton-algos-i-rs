


pub struct UnionFind {
    n: i32, // number of items
}

impl UnionFind {
    pub fn new(n: i32) -> UnionFind {
        UnionFind {
            n: n,
        }
    }

    // fn union(&self)
    //
    // fn connected(&self, a: i32, b: i32) -> bool
}


pub fn union() {
    println!("called `my::function()`");
}
